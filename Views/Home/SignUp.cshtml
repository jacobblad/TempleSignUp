@ViewBag.Title = "Sign Up"

@model IEnumerable<AvailableTime>

@foreach (AvailableTime x in Model)
{
<div class="card">
    <h5 class="card-header">@x.TimeSlot</h5>

    <form id="@x.TimeSlot" asp-action="SignUp" method="post">
        <input type="hidden" asp-for="@x.TimeSlot" />
        <input type="hidden" />
        <span class="card-text p-1">
            @x.TimeSlot
            <button type="submit" class="btn-success btn-sm pull-right" style="float:right">Select Time</button>
        </span>
    </form>


</div>
    <br />
}
